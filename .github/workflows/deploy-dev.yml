name: Deploy Universe Tracker

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  NODE_VERSION: '24'

jobs:
  test:
    name: Lint, Typecheck & Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint
        working-directory: frontend
        run: npm run lint

      - name: Typecheck
        working-directory: frontend
        run: npx tsc --project tsconfig.app.json --noEmit

      - name: Unit tests
        working-directory: frontend
        run: npm test

  deploy-dev:
    name: Deploy to Dev Environment
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Deploy backend to dev
        working-directory: backend
        run: npx serverless deploy --stage dev
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          ACM_CERTIFICATE_ARN: ${{ secrets.ACM_CERTIFICATE_ARN }}
          ADMIN_SETUP_KEY: ${{ secrets.ADMIN_SETUP_KEY }}

      - name: Set Cognito IDs
        id: cognito
        run: |
          echo "user_pool_id=us-east-1_5GZ9UY8V2" >> $GITHUB_OUTPUT
          echo "client_id=5bn99khg7knh9v0t5dqbjpgh1d" >> $GITHUB_OUTPUT

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build -- --mode universe
        env:
          VITE_API_BASE_URL: ${{ secrets.API_BASE_URL }}
          VITE_COGNITO_USER_POOL_ID: ${{ steps.cognito.outputs.user_pool_id }}
          VITE_COGNITO_CLIENT_ID: ${{ steps.cognito.outputs.client_id }}
          VITE_AWS_REGION: us-east-1

      - name: Deploy frontend to S3
        working-directory: frontend
        run: aws s3 sync dist s3://universe.jpdxsolo.com --delete --exclude "*.map"

      - name: Get CloudFront Distribution ID
        id: cloudfront
        run: |
          DISTRIBUTION_ID=$(aws cloudformation describe-stacks \
            --stack-name universe-tracker-api-dev \
            --query "Stacks[0].Outputs[?OutputKey=='CloudFrontDistributionId'].OutputValue" \
            --output text)
          echo "distribution_id=$DISTRIBUTION_ID" >> $GITHUB_OUTPUT

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ steps.cloudfront.outputs.distribution_id }} \
            --paths "/*"

      - name: Wait for CloudFront invalidation
        run: |
          echo "Waiting for CloudFront cache to propagate..."
          sleep 30

      # E2E tests and screenshots temporarily disabled
      # - name: Install E2E test dependencies
      #   working-directory: e2e
      #   run: npm ci
      #
      # - name: Install Playwright browsers
      #   working-directory: e2e
      #   run: npx playwright install chromium --with-deps
      #
      # - name: Run E2E tests
      #   working-directory: e2e
      #   run: npx playwright test --project=chromium
      #   env:
      #     TEST_ENV: dev
      #
      # - name: Upload E2E test results
      #   uses: actions/upload-artifact@v4
      #   if: always()
      #   with:
      #     name: e2e-test-results
      #     path: e2e/reports/
      #     retention-days: 30
      #
      # - name: Capture screenshots of dev site
      #   working-directory: e2e
      #   run: npm run screenshots
      #   env:
      #     SCREENSHOT_BASE_URL: https://universe.jpdxsolo.com
      #
      # - name: Upload screenshots
      #   uses: actions/upload-artifact@v4
      #   if: always()
      #   with:
      #     name: page-screenshots
      #     path: screenshots/
      #     retention-days: 30

      - name: Deployment summary
        if: always()
        run: |
          echo "## Universe Tracker Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend**: https://universe.jpdxsolo.com" >> $GITHUB_STEP_SUMMARY
          echo "- **CloudFront Distribution**: ${{ steps.cloudfront.outputs.distribution_id }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${GITHUB_SHA::7}\`" >> $GITHUB_STEP_SUMMARY
